//Slice
const a = ['a', 'b', 'c', 'd', 'e', 'f', 'g'];
console.log(a.slice(2, 5)); //its mean the last given  value not inclued;

console.log(a.slice(-5, -3)); //first limit included and lasst given limit not included,,...
console.log(a.slice(1));
console.log(a.slice(2, 4));
console.log(a.slice(-6, -3)); //Remember this first limit included and last limit not included in the sequence

//Splice Method
